# 週間計画 — 2026-W35（08/24〜08/30）

**今週の成果**: 3 週間気づかれていなかった商品側の不具合（有料 71 本のプレビュー）に決着をつけ、実害を判定できない計器（PSI field）を判定できる状態に戻す。

**参照**: [monthly.md](./monthly.md) ／ タスクの詳細・完了条件・検証は [backlog.md](./backlog.md) の各 ID を見る（ここには複製しない）／ 週次レビュー: `docs/reviews/weekly/2026-W35-review.md`

---

## 実行タスク

| 優先 | ID | 今週の出口 | 担当 |
|---|---|---|---|
| 🔴 | DN-0126 | 監査の下限を書き込み経路と揃えて測り直し、「本当に下限割れ」の本数を出す。ライブ反映は承認待ちで止める | Claude Code候補 |
| 🔴 | DN-0127 | field が消えた原因を切り分け、field 不在時にゲートが「判定不能」を表明する状態にする | Claude Code候補 |
| 🔴 | DN-0128 | 高流入 9 本のうち R8択一（104users）と PE施工能力（39users）だけでも `magazine-placement.ts` に配線する | Claude Code候補 |
| 🟡 | DN-0003 | 1日 100 件上限で消化。ABORT 分は翌日へ回す | 当方 |
| 🟡 | DN-0129 | 常時緑 4 枚の `[検証:]` を差し替えるか外す（55 枚への付与は次週以降） | Claude Code候補 |

## 手動キュー（ユーザー・別PC／時間差で可）

| 出口 | 備考 |
|---|---|
| ココナラ DM 残 2 件の返信 | W34 の申し送りから落ちていた分。返信送信は運営者（`inquiriesOneWay=3` / `resolved=1`） |
| `npm run coconala-orders` で実体を採り直す | snapshot が 7.5 日前で `check-coconala-orders` が検査不成立。Playwright・ローカル限定 |
| `npm run a8-ui:fetch` で A8 単月取得 | W34 から落ちていた分。前回 8/4 で期限まで 10 日。要 A8 ログイン |
| DN-0007 添削 1 本の工数を実測（30 分以内か） | 会員ローンチ（DN-0019）の律速 |

## 定期（backlog に置かない・`check-*-due` が期限を持つ）

- brain 競合スキャン — 一度も未実施。手動 WebSearch（`check-competitor-scan-due` が `due:true`）
- `/distill-proofread-learnings` の再開 — 3 か月目。ただし今週は `content/site` の差分が薄く入力自体が少ない

## 今週やらないこと

- **DN-0126 の 71 本一括本文修正** — 測り直す前に本文へ手を入れない（下限の食い違いで過検出になっている可能性が高く、必要な本数が確定していない）
- note L1/L2・サイト→note意味導線の再編（DN-0101）— DN-0100 の緊急修復を先に完了する
- note ライブ変更全般 — 対象と差分を提示してユーザー承認を得るまで実行しない
- note 本文 drift の全量消化（DN-0003）— 1 日上限があるため今週で終わらない前提で回す
- X OVERDUE 3 件（DN-0130）— 🟣 判断待ち。内容を読む時間が取れる週に回す
