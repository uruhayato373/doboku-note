# 週間計画 — 2026-W35（08/24〜08/30）

**今週の成果**: 3 週間気づかれていなかった商品側の不具合（有料 71 本のプレビュー）に決着をつけ、実害を判定できない計器（PSI field）を判定できる状態に戻す。

**参照**: [monthly.md](./monthly.md) ／ タスクの詳細・完了条件・検証は [backlog.md](./backlog.md) の各 ID を見る（ここには複製しない）／ 週次レビュー: `docs/reviews/weekly/2026-W35-review.md`

---

## 実行タスク

| 優先 | ID | 今週の出口 | 状態 |
|---|---|---|---|
| 🔴 | DN-0126 | 測り直し → CRITICAL 71 は過検出で **本当の下限割れは 0 本**。workflow green 実測 | ✅ **完了**。21 日赤は 2 層構造で、71 件の下に `npm ci` 欠落（publish 段の gray-matter 不足）が隠れていた。修正して再 dispatch → **conclusion=success を実測**（run 32797779154）。残 = Issue #473 クローズ（ユーザー） |
| 🔴 | DN-0127 | field が消えた原因を切り分け、field 不在時にゲートが「判定不能」を表明する状態にする | ✅ 完了（`field-coverage` 違反型を新設・実走 0/44 で exit 1） |
| 🔴 | DN-0128 | 高流入 9 本のうち R8択一（104users）と PE施工能力（39users）だけでも配線する | ✅ 完了。**その 2 本は W33 時点で配線済みだった**（レポートが `top` を数えていなかった）。真に未配線の 5 本を配線し 9 → 1（残 1 は career 記事で設計どおり） |
| 🟡 | DN-0003 | 1日 100 件上限で消化。ABORT 分は翌日へ回す | ✅ **今日ぶん完了 ok=76 / fail=1**。反映後 `check-note-structure` CRITICAL=0（実検査 809/810・取得失敗 1）。残 23 本＋失敗 1 本は翌日へ |
| 🟡 | DN-0129 | 常時緑 4 枚の `[検証:]` を差し替えるか外す（55 枚への付与は次週以降） | ✅ 完了。実走では **5 枚**だった（カードの 4 枚は陳腐化）。5 枚とも代替コマンドが無く token を除去 |

## 追加 pull（2026-08-25・backlog↔monthly 突合で期限系を移した）

| 優先 | ID | 今週の出口 | 状態 |
|---|---|---|---|
| 1 | DN-0100 | **期日 8/23 超過**。実装指示書の Phase A〜C（総監 evergreen CTA・review候補10本確定・`wire-note-paid-cta` で 1級50+2級24 の L2 帰路配線）→ ライブ対象78本の**承認パッケージ提示**まで。承認後のライブ反映は DN-0003 バッチ枠に合流 | ⬜ 未着手 |
| 2 | DN-0005 | X 9月 week1 投入 | ✅ **19/19 完了**（カード記載の 21 本は誤りで実体 19 本）。実キューで照合済み。**真の障害は CRLF による見出し混入**で、見出し行が本文に残り全ツイートが 280 字ガードに掛かっていた（別 commit で修正）。欠番は `replaced`＝意図的差し替えで欠落ではなかった |
| 3 | DN-0002 | メンバーシップ週次配信 | ✅ **今週分は完了**。学科記述予想 01_土工 を会員限定配信（`n018e626cf66f`・`is_limited=true`／未ログイン0字を実査）。**W6 は出さない** — README の SSOT が「次回は W6（8/31）」と定め、8/31 は W35（08/24〜08/30）の範囲外。6 日早く出すとドリップを崩す。火金トラックは 8/21 開始予定なのに 1 本も出ておらず、今日（火）で立ち上げた。次は 8/28（金） |
| 4 | DN-0109 | CI/CD 信頼性回復 | ✅ **Phase 0 完了・カード縮約**。起票時の確定エラー 4 つは全て解消済みだった。自分が入れた knip +3 を宣言で返済（Unlisted binaries 17→**13**）。`quality:audit:ci` **pass 62 / fail 0**。Phase 1〜5 は承認が要る規模で期日を 9/30 へ |
| 5 | DN-0119 | career WARN 衛生 | ✅ **完了・カード削除**。`HIGH_INTENT_CAREER_SLUGS` へ収録し WARN 1 → **0**。`<CareerAffiliate>` は**足していない** — 配置は `resolveDocsCareerSidebarAd` が category で決めており、このセットは 2026-07-28 以降 arm 判定に使われない純粋な台帳。既に登録済みで未配置の記事も 4 本ある |

## 手動キュー（ユーザー・別PC／時間差で可）

| 出口 | 状態 |
|---|---|
| ~~ココナラ DM 残 2 件の返信~~ | ✅ **対象なし**。実体を採り直したところ DM 4 件は返信不可 3（運営からの一方向通知 1・規約違反で削除済み 2＝相手がアカウント制限中）＋決着済み 1 で、返信が要るものは 0 件 |
| ~~`npm run coconala-orders` で実体を採り直す~~ | ✅ 完了（0.0 日前）。取引 2 件が orders-log と完全一致 |
| `npm run a8-ui:fetch` で A8 単月取得 | ⏳ **ユーザーのみ実行可**（A8 ログイン＋CAPTCHA）。W34 から落ちている。前回 8/4 |
| DN-0007 添削 1 本の工数を実測（30 分以内か） | ⏳ ユーザー。会員ローンチ（DN-0019）の律速 |

## 今週あらたに起票したもの

| ID | 内容 | 起票理由 |
|---|---|---|
| DN-0133 | `placement.sidebar` が描画されないのに導線として数えられている | DN-0128 の調査で発見（レポートの過大評価。`deriveAffiliate` が依存しているので切り出し） |
| DN-0134 | 昇格後に asset-inbox を実走して R3 の納品 PDF 3 本を R2 へ反映 | R2 書き込みを CI 完結にする経路を新設。release は投函済みで main 昇格待ち |

DN-0132（BOUNDARY_SHIFT 270 本）は**専用作業が不要になった**ため縮約した — DN-0003 の反映で
270 → 194 へ減り、減少数が反映本数と一致し**反映済みの残留がゼロ**。誤検出ではなく実ズレで、
再公開すれば消えることが実証されたので DN-0003 の消化に従属する。

## 定期（backlog に置かない・`check-*-due` が期限を持つ）

- brain 競合スキャン — ✅ 実施し `competitors-2026-08-25.json` を作成（`due:true` は履歴 JSON が無かったのが原因で、09§E には 2026-07-20 probe があった）。**ただし探索自体は失敗**: 既知の study_base すら再発見できず「新規参入 0 件」とは書けない。次回はログイン状態のサイト内検索で採る
- `/distill-proofread-learnings` の再開 — ⏭ 見送り。直近 7 日の `content/site` コミット 6 件のうち実質は 総監KW 3 本の公開→Revert と主任技士 guide-essay の CTA 追加のみで、**校正の学びを抽出できる差分が無い**（履歴切り詰めコミットが 1 件混じるため件数は多く見える）

## DN-0003 バッチ実績（2026-08-25）

対象リストは `--json` の `driftFiles` から再生成した（旧 `.txt` は 41 件陳腐化していたため使わない）。
本文 drift は 422 本へ増えており、**画像あり 158 / 画像なし 264**。CDN 確定失敗を避けるため
画像なしのうち有料 261 本から 100 件を投入（無料 3 本は `--commit` の「更新する」検出が
未検証なので今日は外した）。`--reattach-pdf` 付き。

**実績: ok=76 / fail=1 / 投入 100 本**（初回 73/100 → 3 連続失敗で残 23 本は未実行 → 復旧して 3 本追加）

初回の失敗 4 件はどちらも**本文を触らず中断**（fail-closed）で破損なし:

| 中断 | 件数 | 原因 | 処置 |
|---|--:|---|---|
| `--reattach-pdf` の実体不足 | 3 | 納品 PDF が R2 へ退避済みでローカルに無い（private バケット＝公開 URL 無し・この PC に R2 creds 無し） | **spec から再生成**して復旧 → 反映済み |
| 有料境界の H2 を特定できず | 1 | `工事119-小規模マンホール内面更生` に「試験問題/予想問題」H2 が無い | 未処置。`--keep-boundary` は既知事故（2026-07-31）があるので使わず、個別対応にする |

PDF 再生成は `scripts/pdf-specs/BK-01_道路.json`（include DSL）が SSOT。3p/3p/5p・`試験問題` と
`フル模範解答` の両節あり・U+FFFD 0 を実検証してから貼り直した。

再現性は **sha256 で 3 本とも不一致**（当初サイズだけ見て「byte 完全一致」と書いたのは誤り）。
PDF に `creationDate`/`modDate` が埋まるため再生成のたび hash が変わる。ただし II-1/II-2 は
サイズが R2 と一致しており**タイムスタンプ以外は同一**とみなせる。III のみ 2,158 bytes 差＝内容が変わっている。

**残 23 本（未実行）＋失敗 1 本は翌日へ**。本文 drift は 422 → 約 346 本。

反映後の `check-note-structure` は **CRITICAL=0**（実検査 809/810・取得失敗 1）。
あわせて **DN-0132 の切り分けが決着**した — `BOUNDARY_SHIFT` 270 → 194 で減少数が反映本数と一致し、
**反映した 76 本の残留はゼロ**。誤検出ではなく実ズレで、再公開すれば消えることが実証された。
DN-0132 に専用作業は要らなくなり、DN-0003 の消化に従属する。

## 今週やらないこと

- **DN-0014 / DN-0022 / DN-0074**（monthly 選択の制作系・期限 8/31）— 大きさに対して今週の容量が追加 pull 5 件 + DN-0003 継続で埋まるため **W36 へ送る**。monthly に残っているので消えない

- **DN-0126 の 71 本一括本文修正** — 測り直す前に本文へ手を入れない（下限の食い違いで過検出になっている可能性が高く、必要な本数が確定していない）
- note L1/L2・サイト→note意味導線の再編（DN-0101）— DN-0100 の緊急修復を先に完了する
- note ライブ変更全般 — 対象と差分を提示してユーザー承認を得るまで実行しない
- note 本文 drift の全量消化（DN-0003）— 1 日上限があるため今週で終わらない前提で回す
- X OVERDUE 3 件（DN-0130）— 🟣 判断待ち。内容を読む時間が取れる週に回す
