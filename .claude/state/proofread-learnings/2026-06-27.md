# 校正学習の振り返り 2026-06-27

## 分析対象
- 対象期間: `--since "7d"`（2026-06-20 〜 2026-06-27）
- 対象コミット: `.local/r2/posts/` 配下 154 件
- 主テーマ: (1) affiliate-career-only ドリフト除去（本文 prose）、(2) CEM 概念図解 SVG の大量整備、(3) 転職キャリアガイド新設、(4) seoTitle 整合（EXP-003/004）

## 分類サマリー
- 既存原則の適用: 大多数（学習対象外）
- 新規ルール候補: 0 件（原則は成熟・適用フェーズ）
- 既存原則の精緻化: 0 件
- ユーザー嗜好: 0 件（既存 memory でカバー済み）
- ワークフロー改善: 1 件
- 採点・リライト議論候補: 1 件（リライト側 / 機械検知新設 → backlog §7 起票済み）

---

## 既存原則の適用（学習対象外）

今週の校正系作業は、ほぼ全て既存の確立済みルールの**適用**だった。これは原則セットが成熟している健全なサインで、新規ルール抽出は不要。

| 作業 | 件数 | 既存の真実源 |
|---|---|---|
| 記事 figure-*.svg から「試験ポイント/引っかけ」セクションを除去 | 6+ | content-principles §5 L164 / figure-canvas-policy §2.5 #5 |
| 図内の概念名タイトル除去（今週 P11 機械化） | 複数 | figure-canvas-policy §2.5 #4（P11-concept-title） |
| ExamPoint items を最大2件に圧縮 | 2+ | content-principles §7.1 |
| figure-*.svg を固定キャンバス 400×500 へ再レイアウト | 77 | figure-canvas-policy / figure-canvas.json |
| seoTitle の検索意図整合 | 21+ | EXP-003/004（experiments.json） |

---

## ワークフロー改善 / 採点・リライト議論候補（1 件・backlog §7 へ起票）

### figure-*.svg「試験ポイント/引っかけ」の機械検知欠落

**種別**: リライト側（校正ワークフローの機械化）

**観察**: `figure-canvas-policy §2.5 #5` で「figure に試験ポイント/引っかけバー・注記ボックスを入れない」と禁止済みだが、概念名タイトル（P11）と違い detect.mjs に検知ルールが無い。このため SVG ジェネレータが違反図を作り続け、今週だけで 6 件以上が「作成 → 手動除去」で取りこぼされた。

**証拠（2 回以上）**:
- `274adb804` fix(svg): 記事 figure-*.svg から試験ポイント・引っかけ論点セクションを除去（**6 件**）
- `8eac0fe31` で「引っかけポイント付き」figure を新規作成 → `bad239b5e` で直後に「引っかけゾーン削除」（作成直後の自己訂正）
- `dd64ecca0` 労働関係 svg 試験ポイント除去の**取りこぼし**修正

**仮説**: detect.mjs に P11 と同型の検知ルール（P12 候補）を追加すれば pre-commit で赤落ちし、作成→除去の往復を消せる。誤検知設計（閾値）が論点のためコード変更＝即適用せず判断を仰ぐ。

**起票先**: `docs/todo/backlog.md §7「ユーザー判断待ち」`（本セッションで起票済み）。反映先（ユーザー承認後）: `.claude/skills/quality/check-mdx/scripts/rules/svg/detect.mjs` ＋ `.claude/knowledge/reference/figure-canvas-policy.md`（P12 の記載）＋ `check-mdx/SKILL.md`。

---

## 既存 memory でカバー済み（再記録不要）

今週のユーザー指示・嗜好は、いずれも既存 memory の範囲内で新規性なし:

- 「ssot 原則で古い情報はすべて削除」「ドリフトは削除して」 → `feedback_no_overstate_external_specs` ＋ SSOT 規律（information-architecture.md）
- affiliate prose ドリフト除去（添削サービス/講座/SAT を 7 ページから除去）＋ prose ガード構築（PR #283） → `feedback_affiliate_career_only`（本セッションで prose-drift 注記＋ガード追記済み）
- 「再発防止をしようか」 → check-affiliate-prose.mjs ＋ denylist ＋ pre-commit/CI で機械化済み

**1 回観察の汎化候補（次サイクル再観察）**: 「収益/コンポーネントカテゴリを廃止したら、コンポーネント撤去だけでなく本文 prose（定型句・ブランド名・再提案）も同一スイープし機械ガードする」。今回は affiliate 1 カテゴリ 1 回のため 2 回ルール未満。次に別カテゴリ廃止で同パターンが出たら content-principles or feedback memory へ昇格を検討。

---

## 次回参照
- 今回の分析ログ: `.claude/state/proofread-learnings/2026-06-27.md`
- 前回ログ: `.claude/state/proofread-learnings/2026-04-20.md`
- 次回は `--since "HEAD~<N>"` で差分累積を追跡可能
