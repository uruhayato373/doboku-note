# 自動化マップ（2026-08-03 現在）

doboku-note で稼働中・計画中のすべての自動化を一覧にまとめる。

---

## 自動化タイプと使い分け基準

| タイプ | 向いている用途 | 例 |
|---|---|---|
| **GitHub Actions** | 取得・検査・配布・デプロイ、および範囲と検証を固定できるエージェント判断 | PSI / note public API / R2 / deploy / SEO Rank Watch |
| **Claude スキル（手動）** | 意味判断・文章生成・承認・公開修復 | `/quality-cycle` `/weekly-review` `/competitor-review` |
| **Claude Cron** | GitHub Actionsで代替できない定期エージェント判断 | weekly PDCA の判断部分のみ |
| **npm scripts** | Actions とローカル手動の共通実装 | `refresh-indexes` `audit-note-funnel` |

> [!important]
> 機械的に完結する処理の実行主体は GitHub Actions を正とする。意味判断もSEO Rank Watchのように対象・一時出力・適用ゲートを限定できればActionsへ置く。本人ログインセッション、CAPTCHA/2FA、対話やユーザー承認が必要な操作は既存の手動・ローカル経路を使う。

---

## GitHub Actions（主要ワークフロー）

### 稼働中

| ワークフロー | トリガー（JST） | 処理内容 | 出力先 |
|---|---|---|---|
| `cloudflare-deploy.yml` | main push / 手動 | ビルド → Cloudflare Pages デプロイ（**R2 同期はしない**） | doboku-note.pages.dev |
| `ci.yml` | PR to main/develop、develop push | `quality:audit:ci`（型・テスト・MDX・eslint・ラチェット・svg・image・図クロップ写り込み・KaTeX警告・台帳整合 等。**件数と内訳の真実源は `scripts/quality-audit.mjs` の `CHECKS`**＝ここに数値を書くとドリフトする）→ ビルド | GitHub Status |
| `e2e.yml` | UI関連PR / 手動 | Playwright E2E。失敗時のみ trace/report を artifact 化 | GitHub Status / artifact |
| `psi-audit.yml` | 毎日 02:00 / 手動 | PageSpeed Insights 計測（4 並列・fetch 120s timeout）→ しきい値判定。CrUX 0 件のときは URL/origin レベルの内訳を latest-report に出し、判定不能の警告として継続観測する | `.claude/state/metrics/psi/` |
| `note-cover-supply.yml` | develop push（`content/note/**/article*.md`）/ 日曜 07:00 / 手動 | note カバー PNG の欠落を `check-note-cover-coverage --json` で検知 → dir ごと生成 → 欠落分だけ R2 へ供給 → manifest を develop へ commit（ogp-supply.yml の note 版・R2 creds の無い環境で書いた記事のカバーを CI が補う） | R2 ＋ `.claude/state/assets/manifest.json`（develop） |
| `fetch-metrics.yml` | 金曜 06:00 / 手動 | GA4・GSC 週次フェッチ → データ整合性チェック | `.claude/state/metrics/` |
| `seo-rank-watch.yml` | 毎日 09:13 / 手動 | 資格別GSC順位取得→期限判定→選定1記事の検索意図調査・小さな改善→履歴追記。本番反映のhash照合後に7日観察。候補なしも記録 | 既存rank-watch履歴・experiments・変更記事（develop）、Summary、失敗Issue |
| `link-audit.yml` | 金曜 07:00 / 手動 | MDX 内部リンク検証 → レポート生成 | `.claude/state/link-audit/` |
| `index-coverage.yml` | 毎月1日 11:00 / 手動 | sitemap URL Inspection（5 並列・100 件ごと checkpoint・step 内 180 分で中断 flush）→ coverage 履歴。完走しなかった月は部分 batch だけ commit して history には積まず、完全性ゲートで赤にする | `.claude/state/metrics/url-inspection/` |
| `uptime-ping.yml` | 08:00 / 14:00 / 20:00 / 手動 | SSR 健全性監視（HTTP + `<main>` + キーワード確認） | `automation-failure` Issue（失敗時・`report-automation-failure.mjs`・復旧で自動クローズ） |
| `ops-audit.yml` | 毎日 07:45 / 手動 | quality-audit の **ops 区分**（`ops:true`＝会員配信ドリップ、note月次アクセス・売上転記、KDPロイヤリティ取得期限・全書籍紐付け、ココナラ分析鮮度・全件取得、**ig-insights-freshness・cloudflare-metrics-freshness**）を develop で実行。PR のゲートには入れない（壁時計依存の赤は diff で直せない） | `automation-failure` Issue channel ops（遅れがあれば起票・repo owner に assign＝通知メール・全 PASS で自動クローズ。secret `SLACK_WEBHOOK_URL` があれば Slack にも） |
| `fetch-ig-insights.yml` | 手動のみ（Meta 利用制限で Graph API トークンを発行できないため cron 停止） | Graph API で media＋insights を取得（制限が解けたら cron を戻す）。公開状態の照合は `login-collectors.yml`（encrypted-state・`verify-ig-status`）が担う | `.claude/state/metrics/instagram/`（develop）・失敗は Issue channel `ig-insights` |
| `cloudflare-metrics.yml` | 毎日 05:23 / 手動 | Cloudflare GraphQL Analytics でゾーン別日次集計を取得 | `.claude/state/metrics/cloudflare/` |
| `cloudflare-config-audit.yml` | 月曜 06:47 / 手動 | Cloudflare ゾーン設定（キャッシュ/圧縮/WAF/Bot Management）のドリフト検知 | `.claude/state/cloudflare/zone-config-latest.json`（ドリフトは Issue channel cloudflare-config） |
| `r2-sync.yml` | main push（`**/img/**` / `**/ogp.png` / `**/ogp.webp` 変更時）/ 手動 | ローカル画像を Cloudflare R2 にアップロード | Cloudflare R2 |
| `r2-audit.yml` | 月曜 07:00 / 手動 | OGP カバレッジ検査（`check-ogp-coverage`）＋ local(main) vs R2 画像差分監査 | GitHub Step Summary（漏れで赤落ち） |
| `content-quality-audit.yml` | 月曜 07:15 / 手動 | note・記事・図版の週次品質ゲート。R2 監査と独立して実行 | GitHub Status |
| `ci.yml`（Pre-merge check） | develop push / PR | quality:audit:ci → build → check-seo-build:ci | GitHub Status ＋ **失敗時 `automation-failure` Issue**（2026-08-24 追加。それまで赤を読む経路が無く 24 連続赤が 2 日気づかれなかった）・develop push が緑に戻ると自動クローズ（2026-09-18） |
| `note-live-audit.yml` | 月曜 07:30 / 手動 | note public API で構造・タグ・本文・公開状態・マガジン・CTAライブ反映を横断照合 | GitHub Status / artifact |
| `weekly-review-guard.yml` | 月曜 11:17 / 手動 | 週次レビュー欠落（ファイル実在）とオフライン期限（実験・GSC UI・A8 取込・GA4 設定）・設定ドリフト・quality-audit report 区分・workflow health を backstop。週次レビュー本体は 2026-09-19 からローカル実行（土曜・`/weekly-review`）で、クラウドルーティンは退役 | GitHub Status / Step Summary / automation-failure Issue |
| `competitor-scan.yml` | 1/4/7/10月1日 10:17 / 手動 | note / IG / ココナラの公開データを取得し前回比を保存 | 各 `.claude/state/**/history/`（develop） |
| `verify-yt-status.yml` | 金曜 05:00 / 手動 | YouTube 台帳と公開状態の read-only 照合 | `.claude/state/yt-verify/`（develop） |
| `gsc-auto-review.yml` | 金曜 12:00 / 手動 | **GSC 記録層**。claude-code-action（fable-5）が metrics-analyzer / gsc-index-auditor（sonnet）を起動し、週次 performance と月次 coverage の診断を観測ログへ自動追記。反映と commit は決定的な bash・記録が増えなければ赤落ち | `gsc-management.md` 観測ログ＋`improvements/`（develop） |
| `r2-brain-dist.yml` | 手動 | Brain 配布物を R2 へアップロード（dry-run対応） | Cloudflare R2 |
| `r2-delete.yml` | 手動 | 明示リストの R2 object 削除（既定dry-run） | Cloudflare R2 |
| `login-collectors.yml` | cron（`.claude/config/playwright-auth-profiles.json` の各サービス `ci.cron` の和集合）/ 手動 | サービス単位 matrix（1 並列）で暗号化 storageState を復元し、各サービスの `readOnlyScripts` を実行（canary 卒業前は plan 0 件・`ci.canary:true` は cron に載せず dispatch の service 明示のみ） | 各サービスの既存台帳（発行元スクリプトと同じ出力先）。失敗は Issue channel `auth-state-<service>` |
| `ops-write.yml` | workflow_dispatch のみ | note / ココナラ / Brain / KDP / X への**書き込み**を実行する唯一の入口。`.claude/config/ci-write-operations.json`（カタログ・18 操作）にある operation だけ実行可。人が `npm run ops-write:plan` で得た plan hash（`plan_sha256`）を dispatch に渡し、CI が checkout した tree で再計算した hash と一致したときだけ `commit=true` で実行する（不一致は exit 2 で何もしない） | 各操作の `ledger`（develop へ commit）。失敗は Issue channel `ops-write-<service>` |
| `scheduled-publish.yml` | cron 30 分刻み（JST 07:00〜23:30）/ 手動 | 承認済みキュー（`content/sns/x/{draft,published}/*/status.json` の `scheduled`/`queued`）から期日到来分を X へ自動投稿。判定は `scripts/x-publish-scheduled.mjs`＋`scripts/lib/x-frequency-gate.mjs`（頻度ゲート 12 規則）。note/Instagram ジョブは後続ユニットで追加予定 | `content/sns/x` スナップショット・`.claude/state/x-publish`（develop へ commit）。失敗は Issue channel `x-publish` |

> [!note]
> Google Drive を読み書きするワークフローは存在しない（意図的）。人（human tier）のアセットは手元の `drive-vault-sync` だけが触る。真実源 [asset-storage-policy.md](../../.claude/knowledge/reference/asset-storage-policy.md) §1。

### 停止・一時用途

| ワークフロー | 状態 | 理由 |
|---|---|---|
| `post-youtube-scheduled.yml` | GitHub側で手動無効（2026-08-03確認） | 再有効化するまで daily cron は発火しない |
| `cf-cleanup.yml` | one-shot / 手動 | 旧 Cloudflare project 削除専用。完了後は workflow 自体を撤去する |


---

## Claude スキル（手動トリガー）

主要スキルをカテゴリ別に整理。全スキルの一覧・詳細・件数の SSOT は `.claude/knowledge/reference/skills-guide.md` を参照（本 doc は件数を重複保持しない）。

### 運用（weekly PDCA）

| スキル | 用途 |
|---|---|
| `/weekly-review` | 過去 7 日の PSI・品質・学習を集計、PDCA Issue 生成 |
| `/weekly-plan` | open Issue を来週の Must/Should に組み込む |
| `/weekly-improve` | GSC 高 impressions ページ抽出 → 改善候補リスト |
| `/nsm-experiment` | NSM 実験設計・追跡 |

### コンテンツ作成・改善

| スキル | 用途 |
|---|---|
| `/improve-article` | 単一記事の SEO・品質改善 |
| `/quality-cycle` | キーワードページの品質サイクル（score/rewrite/verify/auto-loop） |
| `/pdf-to-mdx` | PDF → MDX 変換 |
| `/keyword-page` | キーワードページ新規作成 |

### デプロイ・開発

| スキル | 用途 |
|---|---|
| `/deploy` | develop → main → Cloudflare Pages デプロイ（post-deploy 検証含む） |
| `/dev-start` | 開発サーバー起動 |
| `/check-mdx` | MDX 構文・品質チェック |
| `/sync-r2-images` | R2 画像差分確認・同期 |

### SNS・note

| スキル | 用途 |
|---|---|
| `/ig-post-create` | Instagram 投稿素材作成 |
| `/create-x-card` | X（Twitter）カード作成 |
| `/publish-x` | X 投稿実行 |
| `/yt-shorts-create` | YouTube Shorts 素材作成 |
| `/pe-note-plan` | note 有料記事の企画・構成 |

---

## Claude Cron（クラウドルーティン）

GitHub Actionsへ移せない意味判断だけを残す。

| ルーティン | 発火 | 残す責務 | CIへ移した責務 |
|---|---|---|---|
| `doboku-note weekly PDCA` | 月曜 | 計測結果の解釈、週次レビュー/計画の文章化、優先順位判断 | 計測取得、機械ゲート、期限surfacer |
| ~~`doboku-note GSC auto review`~~ | — | **退役（2026-08-06・`enabled:false`）**。記録層は `gsc-auto-review.yml` が引き継ぎ自走を確認済み | 記録層ごと `.github/workflows/gsc-auto-review.yml` へ |
| `doboku-note note-funnel monthly audit` | 月次 | なし。意味監査はオンデマンドで実行 | D1-D6 の機械監査を `r2-audit.yml` / `note-live-audit.yml` へ移管 |

> [!note] GSC auto review の設計意図と CI 移行（2026-08-06）
> データ取得は CI で全自動だったが**分析と記録が手動**で、7/1・8/1 の月次カバレッジ 2 回分が
> 無記録のまま indexed_ratio が 81.6%→71.7% に低下していた。「計測 → 記録」のループを
> 人手なしで閉じる。実験の start・タスク起票はせず**推奨まで**（実行判断は人）。
>
> **実行主体はクラウドルーティン → GitHub Actions へ移す**。理由は本表の原則そのもの＝
> クラウドルーティンは設定も実行履歴も repo から見えず、**本機能を作った動機（沈黙が見えない）を
> 実行基盤自身が再現してしまう**。`claude-code-action` で意味判断も CI 実行できるため、
> 「意味判断はクラウドに残す」例外を本件には適用しない。
>
> **移行完了（2026-08-06）**。`CLAUDE_CODE_OAUTH_TOKEN` を登録し、CI で自走を確認したうえで
> クラウドルーティンを `enabled:false` にした。オーケストレータは `claude-fable-5`、重い JSON 走査は
> `.claude/agents/*.md` の frontmatter で sonnet に固定されたサブエージェント（metrics-analyzer /
> gsc-index-auditor）が担う。詳細な分業は
> [gsc-management.md](../../.claude/knowledge/reference/gsc-management.md)。
>
> **CI で Claude を動かすときの落とし穴**（同種の自動化を作るときに再発しやすい・実測）:
> ①workflow_dispatch も cron も**デフォルトブランチ版**で動く ②`gh secret set` の隠し入力に
> 貼り付けが効かず**値が空**で登録されることがある（run ログの env が `TOKEN: ***` か `TOKEN: ` かで判る）
> ③`id-token: write` が必須 ④claude-code-action は workflow が**デフォルトブランチと同一内容**で
> ないと実行をスキップ ⑤**CI は非対話＝turn 終了でセッションも終了**（サブエージェントを待って
> turn を終えると記録が永久に失われる）⑥**`.claude/**` への Write は sensitive file として一律拒否**
> （allowlist 不可。よって Claude は `/tmp` に断片を出し、反映と commit は bash が行う）
> ⑦action は step 終了時に自身の app token を revoke するため、後続の push は `GITHUB_TOKEN` で
> remote を貼り直す。
>
> **設計の要点**: 「Claude が成功終了しても記録が増えていなければ成功ではない」を workflow 自身の
> ゲートにした（実行前後の gates を突合し、週次が未記録なら赤）。可視化（実行ログの artifact 化）を
> 先に入れたから、上の ⑤⑥⑦ を順に特定できた。**緑を信じる前に「何件記録されたか」を見る。**

> [!warning]
> クラウドルーティンの状態は repo に存在しない。旧 note-funnel routine は `/routines`（list-first）で確認し、[Claude Code Routines](https://claude.ai/code/routines) から削除する。週次 PDCA は残し、機械取得や public API 監査を本文から外して判断・文書化だけに絞る。

---

## CI へ移した read-only 取得（encrypted-state）

`login-collectors.yml` が `.claude/config/playwright-auth-profiles.json` の `ci.mode === 'encrypted-state'`
サービスについて `readOnlyScripts` を実行する（canary 卒業後・詳細は
[playwright-auth-profiles.md §CI](../../.claude/knowledge/reference/playwright-auth-profiles.md#ci-encrypted-state2026-09-21)）。

| 処理 | サービス |
|---|---|
| 売上・成果レポート取得 | a8（`fetch-a8-ui-csv` の site-summary / program-detail / period-monthly。period-daily は headless で不可＝ローカル）・coconala（`coconala-orders` / `coconala-analytics`）・note（`note-traffic-fetch`・添付/会員限定の live 検査。**売上 `note-sales-fetch` は購入者一覧のパスワード再確認で hosted CI 不可＝ローカル儀式**）。**hosted CI で成立が実証されたのはこの 3 サービス（2026-09-21）** |
| 読み取り監査 | （2026-09-21 実測で hosted CI 不可に降格: x＝ボット挑戦ページ／google・instagram＝1 回の利用で Google/Meta がセッションを全面失効／kdp＝Amazon が再認証要求／afb＝別プロセスへ持ち出せない。いずれも `ci.enabled:false`。経緯は measurement-incidents.md 2026-09-21） |

## CI で実行するが承認は人（ops-write.yml：dispatch + plan hash・18 操作）

`ci.writeScripts` は `DOBOKU_CI_WRITE_PLAN_SHA256`（ops-write の plan hash）が無いと resolver が拒否する。
人は Mac で `npm run ops-write:plan -- --operation <id> --args '<json>'` を叩いて plan hash を得て
内容を確認し、その hash を `gh workflow run ops-write.yml -f plan_sha256=<hash> -f commit=true` に渡す。
CI は checkout した tree で plan を再計算し、一致したときだけ実行する（Issue channel は `ops-write-<service>`）。
X の予約投稿は `scheduled-publish.yml`（30 分刻み cron・頻度ゲート 12 規則）が別経路で担う。note/IG の
scheduled publish job は後続で追加予定。

| 処理 | サービス |
|---|---|
| 公開・更新・価格・タグ等の書き込み（`ops-write.yml` dispatch） | note・coconala・brain・kdp・x・instagram（`publish-ig-bs` の予約投稿。Meta 利用制限で Graph API 不可）（各 `ci.writeScripts`） |
| 承認済みキューの期日到来分を自動投稿（`scheduled-publish.yml` cron） | x（頻度ゲート付き。note は後続） |

## 移さない

| 処理 | 残す場所 | CI化しない理由 |
|---|---|---|
| Instagram プランナー予約スロットの目視確認 | Business Suite + ローカル | 月ビューの走査は headless で不安定。公開状態照合と予約投稿は上表へ移行済み |
| GA4 管理操作（`ga4-admin:check`） | ローカル Playwright | 外部状態を変える管理操作は本人セッションで人が行う（UI CSV read-only 取得は上表へ移行済み）。GSC の登録リクエストはクロール要求だけでページを変えないため、2026-09-24 に `gsc-request-indexing.yml`（self-hosted runner 限定・DN-0291）へ移す判断をした |
| 初回ログイン・2FA・CAPTCHA | ローカル手動（headed Chrome） | 本人確認は自動突破しない |
| x / google / instagram / kdp / afb のログイン必須処理 | ローカル手動（または Mac を self-hosted runner に） | hosted runner の datacenter IP を各プラットフォームが「別端末」とみなしセッションを失効・挑戦ページを出す（2026-09-21 実測）。residential IP＋同一端末指紋が要る |
| weekly PDCA / 競合意味評価 / note導線意味監査 | 対話エージェント | 優先順位・文章・関連性の判断が必要 |
| `.claude/hooks` / git hooks | ローカル補助 | 編集直後のフィードバックとセッション固有の警告。マージ可否の機械ゲートはCI側でも実行 |

---

## npm scripts（ビルドフロー組み込み）

ビルド・デプロイに自動で呼ばれるスクリプト群。単発でも実行可能。

| コマンド | タイミング | 処理 |
|---|---|---|
| `npm run refresh-indexes` | **コンテンツ編集完了後に必須** | backlinks・cross-exam・タグ インデックスを再生成 |
| `npm run generate-webp` | `npm run build` の prebuild で自動実行 | PNG/JPG → WebP 変換 |
| `npm run quality:audit` | 手動（月次棚卸し）/ `:ci` は ci.yml で自動実行 | コード・記事・画像/SVG の機械チェックを横断実行→レポート |
| `npm run validate-mdx` | CI（ci.yml）で `quality:audit:ci` 経由 | MDX 構文バリデーション |
| `npm run type-check` | CI（ci.yml）で `quality:audit:ci` 経由 | TypeScript 型チェック |
| `npm run upload-images-r2` | `r2-sync.yml` から自動実行 | R2 へ画像アップロード |
| `npm run check-ogp-coverage` | `r2-audit.yml` から自動実行 / 手動 | published 記事の `ogp.png` 欠落を検知（欠落で exit 1） |
| `npm run check-figure-crop` | `quality:audit:ci`（ci.yml）経由 / 手動 | 図クロップの画素検査（隣接図の切れ端 STRAY_SLIVER を baseline 比の新規のみ gate）。詳細 → [image-policy.md](../../.claude/knowledge/reference/image-policy.md) |
| `npm run check-drive-vault` | `quality:audit:ci`（ci.yml）経由 / 手動 | 置き場ルール（audience）と Google Drive 台帳（`drive-manifest.json`）の整合。CI は Drive マウントを持たないため実体照合はできず、設定・台帳の整合（a〜d）だけを判定し「実体検査 0 件」と明示する |
| `npm run build-backlinks` | 手動 or `refresh-indexes` 経由 | バックリンクインデックス再構築 |

---

## 自動化の継続的改善ループ

```
[毎日]  psi-audit / uptime-ping
[週次]  fetch-metrics / link-audit / r2-audit / note-live-audit
[月次]  index-coverage
[四半期] competitor-scan（note / IG / ココナラの機械取得）
          ↓
[週次]  /weekly-review → CI成果物を解釈
[週次]  /weekly-plan → 優先タスクを整理
[四半期] /competitor-review → snapshotを意味評価
          ↓
[随時]  /improve-article / /quality-cycle → コンテンツ改善
[随時]  /deploy → 本番反映 → post-deploy curl 検証
          ↓
[翌日]  psi-audit で効果測定
```

---

## 計画中（未実装）

| 項目 | 概要 | 工数目安 |
|---|---|---|
| `/weekly-improve` → `/improve-article` 連携 | GSC 高 impressions ページのリストを `/improve-article` に渡す。意味判断と公開承認があるため無人CIにはしない | 1〜2h |
| 受験期コンテンツ補強パイプライン | GSC impressions 急上昇を検知 → `/improve-article` 並行補強 → deploy | Phase 2 着手後 |
| SNS→サイト改善ループ | `/weekly-improve` → `/improve-article` → SNS 素材生成 → 投稿 を1フローに統合 | Phase 2 着手後 |

---

## 関連ドキュメント

| 参照先 | 内容 |
|---|---|
| `.claude/knowledge/reference/workflows.md` | 週次運用フロー詳細・ブランチ戦略 |
| `docs/operations/01_パフォーマンス監視.md` | PSI 計測・3層データモデル |
| `.claude/config/psi-config.json` | PSI しきい値・対象 URL リスト |
| `.claude/todo/backlog.md` | 全タスクの単一正源（`DN-####`。GitHub Issue は `automation-failure` のみ例外） |
