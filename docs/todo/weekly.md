# 週間計画 — 2026-W34（08/17〜08/23）

**今週のゴール**: BuildJob 増額キャンペーン終了（8/31）まで残り 2 週。**1級土木一次の合格発表は 2026-08-13 に済んでおり、二次(10/4)の買い場は既に開いている**（残り約 7 週）。週前半の 3 点（①高流入ページの収益導線 ②売上ログ復旧 ③GA4/GSC 乖離の分離）は 8/17 に決着したので、**後半は「配線したのに出ない」CTA の解消と二次の買い場づくり**へ移る。
**参照**: [monthly.md](./monthly.md)（8月＝1級二次対策 仕込み期／BuildJob 最終月）、[W33 レビュー](../reviews/weekly/2026-W33-review.md)

---

## 今週やること

| 優先 | タスク | 担当 | 状態 |
|---|---|---|---|
| ✅ | ~~高流入ページの note 導線ゼロを解消~~（W33 Must #2）。総監 択一 r0X/h2X 全18本＋建設部門コンピテンシー改訂に冒頭CTAを配線 | 当方 | **完了**（[PR #468](https://github.com/uruhayato373/doboku-note/pull/468) / [PR #469](https://github.com/uruhayato373/doboku-note/pull/469) を 8/17 マージ）。**deploy 待ち**＝本番反映はまだ |
| ✅ | ~~売上ログの空白を埋める~~ | 当方 | **完了（8/17）**。142 → **280 件**へ復旧（7月を実体で差し替え・8月を新規記録）。`updatedAt` 8/17 で `check-sales-freshness` は緑。残るのは**取得スクリプトの committed 化**のみ（backlog 🔴 Phase B-5） |
| ✅ | ~~NSM 下落の前年同期比検証~~ → **決着（8/17）**。季節性で説明がつき計測異常ではない。試験日（2級一次 6/7・1級一次 7/5・技術士二次 7/19-20 で山が終わり、次は 1級二次 10/4）と一致し、売上も ¥275,140→¥39,520 と同形で落ちている。GSC の +4 クリックは GA4 の 1/20 の桁で誤差。**前年同期比は 2025 年データが 1 件も無く実行不可能だった**のが 4 週持ち越しの真因 → [measurement-incidents.md](../../.claude/knowledge/reference/measurement-incidents.md) | 当方 | 完了 |
| ✅ | ~~死守コア① 完全攻略パックの追録充実~~ | 当方 | **完了（8/17 実査）**。105 本すべて `noteStatus: published`・未公開 draft ゼロで**商品側の残作業は無い**（backlog 🔴「二次10/4 直前スプリント」①）。死守コアで残るのは ②会員ローンチ（ユーザー律速）③最小リスト捕獲 |
| 🔴 | **1級・2級土木 primary 29ページの死に配線を解消**（backlog 🔴）。`magazine-placement.ts` の分岐13/14 は `inline` のみで、`midEligibleGroup` に `primary` が無いため**note CTA が実質ゼロ**。同型欠陥は 4 度目。**一次合格発表(8/13)後なので二次商品を lead に**（10月中旬まで期間限定） | 当方 | 未着手 |
| ✅ | ~~ココナラ DM 3 件の返信~~ → **対応不要**（1件は完了済み取引の見積り DM、2件はフィッシングで運営がアカウント制限済み＝返信が構造的に不可） | 当方 | **完了（8/17）**。除外条件も `resolved-inquiries.json` の allowlist ＋機械判定で実装済み（**要対応 4→0**）。4テーマ版の再取り下げ監視のみ継続 |
| ✅ | ~~X カウントダウン 064-067 の退役~~（3週連続） | 当方 | **完了（8/17）**。`docs/sns/x/draft/_archive-2026-pe-countdown/` へ退役し偽 OVERDUE は停止。**代わりに未投入 35 件（068/080/082）が可視化された**ので、そちらの処遇判断へ差し替え（backlog 🔴） |
| 🔴 | **FLOW 週次配信 W3+W4 を二本立てで公開**（会員特典マガジン `mbe07bd5cecda` へ収録）。8/5 に初の入会（通年 ¥1,480）があり、プランが約束する配信が W2 で止まっている＝**解約リスク＝直接の収益毀損**。以降 W5 8/24・W6 8/31 と週1で 10/4 に間に合う | 当方 | 未着手 |
| 🔴 | **ココナラブログ 残 2 本の公開**（`hinshitsu-kanri-kakikata` / `sokan-shutsudai-theme-bunseki`）。**期限 8/16 を超過**。公開済みの回が次回内容を名指しで予告しており、遅延はライブの約束を破る。1日1本・順序は変えない | 当方 | 未着手 |
| 🔴 | **CTA 到達検査を実描画へ同期**（Phase E）。`check-magazine-cta-reachability.ts` が `page.tsx` のロジックを複製していて 4 点ズレ。これを直さないと死に配線の 5 度目が止められない | 当方 | 未着手 |
| 🟡 | EXP-006 残り10本のインデックス登録リクエスト（`check-experiment-due` が DUE 判定） | 当方 | 未着手 |
| 🔴 | YT `pending_overdue` 171 件の棚卸し（backlog は 🔴）。台帳（`.claude/state/yt-verify/latest.json`）が 8/7 生成で鮮度も落ちているので**再生成してから** | 当方 | 未着手 |
| 🔴 | note 本文 drift 353 本のライブ反映（backlog は 🔴。1日100件上限＝最低4日分）。**Phase 1 で `src/` を直してから**流す（先に流すと古い CTA を本番へ送って二度手間） | 当方 [Codex候補] | 未着手 |

### 手動キュー（ユーザー／別PC・時間差で可）

- **添削 1 本の工数実測（30 分以内か）** — 会員ローンチの律速。定員・価格が確定しないと募集できず、二次(10/4)の買い場を逃す。**手動キューの最優先**
- **LINE 公式アカウント開設**（ノーコード・器のみ）— 死守コア③「最小リスト捕獲」。磁石PDF・配信台本・友だち追加CTA は当方が作る
- **note 売上ダッシュボードのパスワード認証** → 売上ログ自体は 8/17 に復旧済みなので緊急度は下がった。次は**取得スクリプト（Phase B-5）を組むため**に要る
- Gmail 転送＋フィルタ（`dobokunotecom` 宛を MCP から読めるようにする・backlog 🔴 に手順あり）
- **`civil-1-takuitsu-pdf` の公開可否を判定** — backlog は「KDP Select 明け(~10/06)まで非公開」としているが**実体は published:true / noteUrl 実在**。規約リスクか台帳の陳腐化かの判定はユーザー領域（下記メモ参照）

---

## 今週やらないこと

- 広い表のモバイル横スクロール対応・フロントエンド土台リファクタ（`page.tsx`／`globals.css` を触るため、UI 系の並行セッションと衝突しないタイミングで別途）
- IG 論点パック残 92 件の波状予約（1セッション30件上限・別セッション）
- BK-09/10 R08 予想（クラウド実行枠。今週はローカル作業が主）
- コンクリート診断士 cd-essay（来年7月向け）／iOS アプリ（Web 月収 ¥15k 達成後）

---

## メモ・ブロッカー

- **売上ログの取得経路**: note の「売上管理」`/sitesettings/salesmanage` と「販売履歴」`/sitesettings/purchasers` は、どちらも `note.com/dashboard/*` へ遷移して**パスワード再確認**を要求する（2026-08-17 実測）。パスワード入力はエージェントの禁止行為なので、ここは人が通す。認証がプロファイルに残る期間は未検証。**8/17 は人が通して 280 件へ復旧済み**だが、取得手順が使い捨てで残っていない（→ Phase B-5）。なお `check-sales-freshness.mjs:116` は復旧手段として `npm run note-sales-fetch` を案内するが、**そのスクリプトはまだ存在しない**（壊れた案内）。
- **`civil-1-takuitsu-pdf` が台帳と実態で食い違う**: backlog 🔴「note施策A」は `published: false` / `noteUrl: ''` を前提に「KDP Select 明け（A-01 = 2026-07-08 + 90日 → ~2026-10-06）まで公開しない」としているが、実体は `note-magazines.ts:964` が `published: true` / `noteUrl: n155093f42183`、記事も `noteStatus: published`。**(a) 既に公開済みで backlog が陳腐化しているだけ**か、**(b) Select 独占期間中の先行公開＝規約リスク**かで対応が真逆になる。同内容の Kindle `e-02` は `in_review`（ASIN 未発行）で、Select が効いているのは別タイトルの A-00〜A-06。**判定はユーザー**。
- **CTA が「配線してあるのに出ない」構造**: `placement.inline` は MidCta の供給源だが、`page.tsx` の `midEligibleGroup` は `guide/pillar/textbook/civil-secondary` 限定。**`pastExam` と `primary` は含まれない**ため、その 2 グループは `top`（冒頭CTA）でしか出せない。concrete 2 件（2026-07-31 / 08-13）と今回の総監択一で 3 度目。**同じ理由で 1級・2級土木 primary の分岐（13/14）も現状どこにも描画されていない**（backlog 🔴 に起票）。
- **BuildJob キャンペーン終了 = 2026-08-31**（残り 2 週）。復帰先は 2026-08-04 に建設JOBs 100% へ変更済み。
  **A8 単月取得は 2026-07-28 に実装完了・2026-01〜07 もバックフィル済みで、分母は既に供給されている**
  （monthly.md と W31 weekly.md の「A8 ログインが最優先」は古い記述。8/17 に backlog と突合して判明）。
  EPC が出ない原因は**分子**＝確定報酬が全月ゼロであること（唯一の発生 1 件 ¥50,000 は cancelled）で、
  これは実装では埋まらず成約が要る。判定基準 → affiliate-operations.md §6.5。
- **weekly.md が W31 のまま 3 週未更新だった**（W32・W33 は週次レビューのみ作成）。レビューは `docs/reviews/weekly/` に残っているので経緯は追える。
- **並行セッション注意**: push 前に `git log origin/develop..HEAD` で巻き込み確認。commit は pathspec 明示（`git add -A` 禁止）。
