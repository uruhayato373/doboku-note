# 週間計画 — 2026-W34（08/17〜08/23）

**今週のゴール**: BuildJob 増額キャンペーン終了（8/31）まで残り 2 週、月末には 1級土木一次の合格発表＝二次モードへの転換点。その手前で**「数字が読める状態」を取り戻す**のが今週の主眼。具体的には ①高流入ページの収益導線を埋める ②1ヶ月欠けている売上ログを復旧する ③GA4 と GSC の乖離を分離する の 3 点。
**参照**: [monthly.md](./monthly.md)（8月＝1級二次対策 仕込み期／BuildJob 最終月）、[W33 レビュー](../reviews/weekly/2026-W33-review.md)

---

## 今週やること

| 優先 | タスク | 担当 | 状態 |
|---|---|---|---|
| 🔴 | **高流入ページの note 導線ゼロを解消**（W33 Must #2）。総監 択一 r0X/h2X 全18本＋建設部門コンピテンシー改訂に冒頭CTAを配線。r08-primary は GA4 全ページ1位（481 users）で、文脈一致の択一PDF ¥980 が placement から未参照だった | 当方 | **PR #468 レビュー待ち**（実装・実描画検証済み） |
| 🔴 | **売上ログの空白を埋める**（7/14 以降・1ヶ月超）。142 件・最終エントリ 2026-07-14 を実測。8月がまるごと未記録で、撤退判断も EPC 判定も土台が欠けている | ユーザー認証 → 当方が正規化 | **ブロック中**（note 売上系ページがパスワード再確認を要求。下記メモ参照） |
| 🔴 | **NSM 下落の前年同期比検証**（W33 Must #1・4週連続の持ち越し）。GA4 Organic は 3 週で −30%/週、GSC は clicks +18%・CTR +1.3pt・順位改善で符号が逆。先に GA4 UI 取得の `property-mismatch` を直す必要がある | 当方（ローカル・Playwright） | 未着手 |
| 🟡 | ココナラ DM 3 件の返信（5〜6 日既読のまま実測）。取り下げ→再出品の直後で反応の遅れが効く。併せて 4テーマ版の再取り下げ監視 | ユーザー（送信は運営者） | 未着手 |
| 🟡 | X カウントダウン 064-067 の**退役**（3週連続）。投入ではなく退役が正しい対処。放置すると surfacer が毎週偽の OVERDUE を出し本物の警告を埋もれさせる | 当方 | 未着手 |
| 🟡 | EXP-006 残り10本のインデックス登録リクエスト（`check-experiment-due` が DUE 判定） | 当方 | 未着手 |
| 🟡 | YT `pending_overdue` 171 件の棚卸し。台帳（`.claude/state/yt-verify/latest.json`）が 8/7 生成で鮮度も落ちているので**再生成してから** | 当方 | 未着手 |
| 🟢 | note 本文 drift 353 本のライブ反映（1日100件上限＝最低4日分）。今週から刻まないと来週も同じ数字を見る | 当方 [Codex候補] | 未着手 |

### 手動キュー（ユーザー／別PC・時間差で可）

- **A8 ログイン ＋ `--probe-period --headed` の実機立会い【8/31 まで】** — 9月の EPC 判定（BuildJob→GKS 切替）の分母。3 週間ブロックされており、キャンペーン終了後では取り返せない。**手動キューの最優先**
- note 売上ダッシュボードのパスワード認証（🔴 #2 の前提）→ 認証後に販売履歴をコピペ、または認証済み状態で当方に読ませる
- Gmail 転送＋フィルタ（`dobokunotecom` 宛を MCP から読めるようにする・backlog 🔴 に手順あり）
- ココナラ DM 3 件の返信送信

---

## 今週やらないこと

- 広い表のモバイル横スクロール対応・フロントエンド土台リファクタ（`page.tsx`／`globals.css` を触るため、UI 系の並行セッションと衝突しないタイミングで別途）
- IG 論点パック残 92 件の波状予約（1セッション30件上限・別セッション）
- BK-09/10 R08 予想（クラウド実行枠。今週はローカル作業が主）
- コンクリート診断士 cd-essay（来年7月向け）／iOS アプリ（Web 月収 ¥15k 達成後）

---

## メモ・ブロッカー

- **売上ログが読めない**: note の「売上管理」`/sitesettings/salesmanage` と「販売履歴」`/sitesettings/purchasers` は、どちらも `note.com/dashboard/*` へ遷移して**パスワード再確認**を要求する（2026-08-17 実測）。パスワード入力はエージェントの禁止行為なので、ここは人が通す。認証がプロファイルに残る期間は未検証。
- **CTA が「配線してあるのに出ない」構造**: `placement.inline` は MidCta の供給源だが、`page.tsx` の `midEligibleGroup` は `guide/pillar/textbook/civil-secondary` 限定。**`pastExam` と `primary` は含まれない**ため、その 2 グループは `top`（冒頭CTA）でしか出せない。concrete 2 件（2026-07-31 / 08-13）と今回の総監択一で 3 度目。**同じ理由で 1級・2級土木 primary の分岐（13/14）も現状どこにも描画されていない**（backlog 🔴 に起票）。
- **BuildJob キャンペーン終了 = 2026-08-31**（残り 2 週）。復帰先は 2026-08-04 に建設JOBs 100% へ変更済み。A8 実績が入らないと 9月以降の EPC 判定が実行不能。
- **weekly.md が W31 のまま 3 週未更新だった**（W32・W33 は週次レビューのみ作成）。レビューは `docs/reviews/weekly/` に残っているので経緯は追える。
- **並行セッション注意**: push 前に `git log origin/develop..HEAD` で巻き込み確認。commit は pathspec 明示（`git add -A` 禁止）。
