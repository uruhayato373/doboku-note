# 2026-08-11 note 納品欠落の復旧と再発防止

購入者から「PDFが無い」とコメントで指摘され、全量実査したら **136本が未着**だった件。
恒久ノウハウは各 SSOT へ反映済み（下の「抽出済み」参照）。**ここに残すのは再開手順と未解決だけ。**

## 明日いちばんにやること

```bash
node scripts/note-attach-batch.mjs --commit
```

- 残 **41本**。done-log（`.claude/state/note-attach-done.json`）から自動で再開する
- 同時に、負債 `nded084d4f646`（下記）も再添付されて自動解消する
- 完了後に必ず: `npm run check-note-attachments:live` → `missingPromised: 0` を確認
- 終わったら `.claude/state/note-attach*.json` を commit

## 未解決

| 件 | 状態 |
|---|---|
| 添付 残41本 | 1日100件上限で本日打ち切り。**live は無傷**、購入者影響は残41本のみ |
| 負債 `nded084d4f646` | 上限到達で復元できず `note-attachment-loss.json` に記録。**live は充足1/1で無傷**。明日の batch で消える |
| バナー差し替え 176本 | 「採点者」表記の修正がライブ未反映。**今日94本を添付したので `--reattach-pdf` が必須になり、1本あたり画像+PDFで上限に当たる＝4日規模**。着手前に再見積もりすること |
| サイト `/deploy` | バナー修正の本番反映が未実施 |
| 建設部門の売上2件 | 8/10・8/11 の購入（鉄道・道路まるごと）が sales-log 未記録。金額は note 売上管理で確認 |
| 2級土木の未添付 | 上記41本に含まれる |

## 今日やったこと（結果だけ）

- **BK-I 建設部門 7本を添付**（購入者 yobo 様の指摘分）→ 7/7 実査確認 → コメントで返信済み
- 全量実査 463件（取得失敗0）→ **未着136本を確定**（`missingPromised 136 / missingSilent 0`）
- **94本を添付**（上限で自動停止・残41）
- ココナラ購入者評価 2件送信（★5・実査確認）
- クリエイター名 `dc i` → `doboku-note` 復旧
- 「発注者＝採点者」の事実誤認を全面訂正（画像197枚 + テキスト440箇所）

## 抽出済み（ここには書かない・SSOT を見ること）

| 内容 | 反映先 |
|---|---|
| 添付欠落の検知・週次配線 | `scripts/check-note-delivery-due.mjs` / weekly-review SKILL |
| 添付が失われる4経路と負債記録 | `scripts/note-update-body.mjs` のコメント / `note-operator` エージェント |
| 「79本は添付しない」の撤回と適用範囲 | `noteコンテンツ計画.md` §7 |
| 評価フォーム・コメント返信の実機仕様 | `coconala-operations.md` / `note-comment-reply.mjs` |
| メンバーシップの会費変更不可・作り直し手順 | memory `note-membership-publish` |

## 判断の前提（次のセッションが誤らないために）

- **note 記事の本文更新は手作業でやらない**（運営者方針）。手作業だと添付消失が記録されない
- **メンバーシップがあるのは 1級・2級土木だけ**。技術士（建設部門・総監）には無いので
  「閲覧＝会員／所有＝買い切り」の線引きは適用されない
- 「添付しない」を選べるのは **本文が PDF を約束していない** ときだけ
