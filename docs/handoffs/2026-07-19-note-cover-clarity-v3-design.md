# Codex/Claude 実施ログ：note カバー Clarity V3 設計

> [!done]
> **2026-07-19 完了**：noteカバーの可読性改善案を、既存G2と後方互換なopt-in variantとして実装できる設計書とClaude Code用プロンプトへ落とし込んだ。実装・画像再生成・公開反映は未実施。

## 背景

現行カバーは複数要素が同時に主張し、note一覧の小さなサムネイルで主題を瞬時に把握しづらいケースがある。「落ちる答案 4つの型」を代表例として、情報階層を絞ったモックの実装仕様が必要になった。

## 実施内容

- `docs/design-system/note-cover-clarity-v3.md`
  - `cover.variant: clarity` による後方互換方式を定義。
  - frontmatter、1280×670レイアウト、中央630×630セーフゾーン、文字フィット、変更対象、検証、完了条件を記載。
- `docs/project/05_プロダクト/claude-code-note-cover-clarity-v3-prompt.md`
  - Claude Codeへそのまま渡せる実装指示を作成。
  - 代表記事1件のみ適用し、公開操作を行わないスコープを明記。
- `docs/todo/backlog.md`
  - Clarity V3の代表記事実装を「🟡 中／UI・UX」の実装待ちタスクとして登録。
  - `元発注者の視点で解説` を正規表現とし、「添削者」「添削者視点」を訴求に使わない制約を明記。

## 検証

ドキュメント作成のみ。実装コマンド、画像生成、テストは未実施。文書の差分確認として次を実施する。

```bash
git diff --check
git status --short
```

## 後続メモ

- 著者は元発注者であり添削者ではない。カバーの訴求は `元発注者の視点で解説` とし、「添削者」「添削者視点」は使わない。
- 実装時は代表記事1件で通常版・中央クロップ・320px相当を目視する。
- `--debug-safety` 実行後、通常版を再生成して赤枠を残さない。
- note.com上の公開済みカバー差し替えは別工程であり、今回の指示には含めない。
