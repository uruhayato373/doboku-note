# ハンドオフ: 1級土木 過去問図の「答え漏らし写り込み」是正

作成: 2026-07-08 / 状態: **方針決定待ち（じっくり検討）**。2ページ着手済み・残りは大規模手作業のため保留。

## 背景（発見の経緯）

admin ギャラリー（記事図版タブ）で 1級土木 primary（過去問1次）の図クロップ品質が低い、という指摘から調査。実体は「クロップが甘い」ではなく、より重い **答え漏らし** だった。

図は問題PDFでなく**解答・解説資料**からクロップされており、図の上下に本文が写り込んでいる。その中に

> 「したがって，⑵は適当である。」 「【正解】⑷」

など**正答を明示する記述**が含まれる（公開中ページで受験者に答えが見えている＝ `docs/reference/image-policy.md` の答え漏らし禁止に違反）。加えて問題文（「適当なものはどれか」）・凡例外テキストの写り込みも多い。

## 3つの重要な制約（実証済み）

1. **スキル再抽出は不可能**: 問題PDF（`docs/textbook/１級土木施工管理技士/過去問/{H30,R01,R02,R07}/…問題A.pdf`）は**完全テキストのみ**（"DHP-A.smd" ＝テキスト起こし版・埋め込み画像0・pdftoppm でも図が出ない）。**H26/H27 は PDF 自体なし**。→ 図は**解答資料側にしか存在しない**ので、`/civil-figure-rework`（問題PDFから切り出す設計）は全ページで空 spec を返す。**このスキルは本件に使えない**。
2. **自動クロップは信頼不可**: OCR で文章行を検出して切る方式は、図の**凡例**（"Uc≥10：粒度分布がよい。" 等、句点を含む正当な図要素）を文章と誤認して**図を切りすぎる**。問題文が図の高さまで及ぶページでは逆に**図をクリップ**する。→ **1図ずつ目視の手作業が必須**。
3. **一部は切り直し不可**: 元クロップ時点で図が既に見切れている図（例: `h30-a-fig-04` の図2/図3、`r01-b-fig-03` の網目工程図）は、元図が手元に無いため**切り直しでは直せない**。→ **SVG再描画 or 除去**が必要。

## triage 結果（機械分類）

`.local/r2/posts/civil-construction-1/primary-*/img/*.png` 約108枚を tesseract(jpn) OCR + 答え/句点で分類（スクリプトは当時 scratchpad の `triage-primary-figs.mjs`。再実行可）:

- **LEAK 21**（答え/問題文の写り込み語あり）: `h26-a`(01,03,04,10) / `h27-a`(03) / `h30-a`(04,05,07,09,12) / `h30-b`(01,02) / `r01-a`(01,02,03,05) / `r01-b`(03) / `r02-b`(03) / `r07-a`(01,02)
- **PROSE 26**（句点2+の本文写り込み・品質）: h26-a,h26-b,h27-b,h28-a,h28-b,h29-b,h30-a(02,03,06,11,14),r01-b,r02-a,r02-b,r04-b,r05-b,r06-b,r07-a,r07-b 等
- **MAYBE 11 / CLEAN 53**（CLEAN は良好）

> [!note] 補足
> 「適当なものはどれか」は**問題文**（答えを明かさない）、「したがって，(N)は適当」「【正解】(N)」が**答え漏らし**。LEAK にはこの2種が混在するが、**どちらも図から除去すべき**（figure は diagram + キャプションのみが正）なので、修正手順は同じ。円囲み数字(⑵⑷)で OCR が不安定なため、答え/問題の機械的区別は当てにしない。

## 完了済み（コミット）

| ページ | 図 | commit |
|---|---|---|
| `primary-r01-a` | fig-01/02/03/05 | `a607a36a8` |
| `primary-h26-a` | fig-03/04/10 | `1b33fb09f` |

各図: 上部テキスト帯を除外してタイト再クロップ → OCR で答え語ゼロ確認 → png/webp 上書き → **MDX の width/height を新寸法へ更新** → 1ページ1commit。正答は従来どおり `<details>` 解答・解説に存置。孤児図（MDX 未参照。r01-a-fig-04, h26-a-fig-01 等）は触らず復元。

## 見切れ確定（needs-source・再クロップ不可・2026-07-09追記）

目視スポットチェックで **元図が上端で見切れ、答えの一部が欠落**する図を確定。写り込みではないため OCR は `clean`、鮮明なので機械は `ok` 判定＝機械監査では永久に拾えない。`figure-sources.json` の `manual_needs` に登録し provenance を `rescan-need-source` に上書き済（ギャラリー対応バッジに理由 tooltip 表示）。

| 図 | 欠落 | 根拠 | 備考 |
|---|---|---|---|
| `r04-b-fig-02` | 作業 **B・D**＋イベント**①** | 設問「作業A〜J」・解説「当初CP＝**A→B→D→I**」なのに図に B/D/① 無し。⑤ に上から刺さる矢印の元ノードが上端切断 | **公開中**・`r05-b-fig-02` と md5 完全一致 |
| `r05-b-fig-02` | 同上（同一ファイル） | r04-b-fig-02 の複製 | 公開中 |
| `r01-b-fig-03` | 網目工程図の一部 | 元クロップ時点で見切れ（本handoff §制約3 記録） | 公開中・従来 recrop 誤分類を是正 |
| `r07-b-fig-02` | 作業 **C・D** ラベル＋イベント③上半分 | 正答(選4)が「作業Dが3日遅れると…」で作業D特定を要すのにラベル欠落 | 公開中・トポロジ/日数は残存＝解答検証に影響 |
| `r06-b-fig-02` | 作業 **C・D** ラベル | 解説は node-path(⓪→①→②→③→⑤→⑨)＋F/K 依存で解答可 | 公開中・重症度低（ラベルのみ欠落） |

**工程表10図の全数目視結果**（alt に「工程表/ネットワーク」を含む civil-1 primary 図）: 上記5図が見切れ、残5図（`h28-b-fig-02`/`h29-b-fig-03`/`h30-b-fig-02`/`r02-b-fig-03`/`r03-b-fig-02`）は完全（ノード⓪–⑨・作業A–K・宙ぶらりん矢印なし）と確認。特に `r03-b-fig-02` は r04-b/r05-b が切り落とした上枝 B-①-D を完全表示しており、クリップ診断の裏付けになった。

→ **再クロップでは直せない**（元画素なし）。完全な元スキャン入手 or（トポロジ＋全日数が MDX 記載で確定できる工程表なら）オリジナル SVG 再作図が選択肢。ユーザー判断で **needs-source 棚上げ**（2026-07-09）。機械可読は `figure-sources.json` の `manual_needs`（provenance→ギャラリー対応バッジ tooltip に反映済）。

## 残作業

- **残 LEAK 約14図**（上表から r01-a・h26-a を除いた分）。品質基準まで広げるなら **PROSE 26図**も同種処理。
- うち **切り直し不可の数枚**（`h30-a-fig-04` 図2/3、`r01-b-fig-03` 等）は SVG/除去へ。
- ページ型の難易度差:
  - **易（簡単トリム）**: 図が1ブロック＋上下にテキスト帯（r01-a/h26-a/r07-a 型）→ 上 or 下を切り落とす。
  - **難（page-dump）**: 1枚にヘッダ＋複数図＋答え＋次問題が混在（`h30-a` 型）→ 目的の図領域だけを慎重に切り出す。

## 実行手順（手動・1図ずつ）

```bash
# 1. 現図を確認（webp→png変換して Read）
sips -s format png <img.webp> --out /tmp/x.png   # Read で目視、テキスト帯の位置を判断
# 2. 上帯 or 下帯を除外して切り出し（frac は目視で調整、-trim で外周白を詰める）
magick <src.png> -crop ${W}x$((H-y0))+0+${y0} +repage -trim +repage -bordercolor white -border 12 <out.png>
# 3. OCR で答え/文章が消えたか検証
tesseract <out.png> - -l jpn --psm 6   # 「したがって」「適当」「正解」等が消えたこと
# 4. 実ファイルへ適用 + webp 生成
cp <out.png> <real.png>; magick <real.png> -quality 82 <real.webp>
# 5. MDX の width/height を新寸法へ更新（重要・アスペクト比崩れ防止）
# 6. node .claude/scripts/audit-exam-figures.mjs で broken_image_reference:0 確認
# 7. ページ単位でコミット（明示 pathspec・並行セッション注意）
```

## 決めてほしい論点（じっくり）

1. **範囲**: LEAK 21 だけ直すか、PROSE 26 も含めた全写り込み（約47図）を直すか。
2. **切り直し不可の図**: SVG再描画（image-policy 準拠のオリジナル作図）にするか、除去（本文＋`<details>`で言語化して図なし成立）にするか。
3. **配分**: 手作業40図規模なので、複数セッションに分けるか、一気にやるか。優先は公開中×流入の多いページ（GA4 popular-pages が空だったので別途取得要）。

## 参照

- memory `[[project_civil1_figure_answer_leak]]`（worklist の機械可読メモ）
- `docs/reference/image-policy.md`（答え漏らし禁止・写真SVG化禁止・技術図のSVG化は可）
- `.claude/skills/quality/civil-figure-rework/`（**本件では使えない**＝問題PDFに図が無いため。記録として）
- `.claude/scripts/audit-exam-figures.mjs`（図↔MDX 整合監査）
- 元PDF: `docs/textbook/１級土木施工管理技士/過去問/`（問題PDFは図なしテキスト版。解答資料は別途要確認）
