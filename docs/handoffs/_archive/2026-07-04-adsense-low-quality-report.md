# AdSense「有用性の低いコンテンツ」— 低品質ページ抽出と対応方針

作成: 2026-07-04 / 対象: doboku-note 全 1,051 URL / 却下理由: 「有用性の低いコンテンツ」（直近1-2ヶ月で2-3回）

---

## 結論（先に）

1. **主因は「薄い/独自性の低いページがサイトの25%を占める」こと**。Google URL Inspection（2026-07-01, 全1,051 URL）で **265本（25.2%）が非インデックス**。Google 自身が「クロール済み - インデックス未登録」240本＋「検出 - 未登録」25本と判定している。AdSense の「有用性の低いコンテンツ」はこの構造をほぼ直接反映する。
2. **画像クロップは主因ではない**。懸念された極小PNG 8枚を実測 → 解像度は 500〜1215px 幅で十分、小サイズは単純な線画ゆえの高圧縮。過去の審査分析にも画像指摘なし。→ **今回の審査対策から画像は外してよい**（200×180の2枚のみ小さいが機能はしている）。
3. **内部品質スコアは当てにならない**（最重要）。非インデックスの166本のCEMキーワードページは **全て内部ルーブリックに合格**（weighted 2.2〜3.0・うち54本が2.8以上）しているのに Google はインデックスしていない。内部ルーブリック（構造30%・モバイル25%・原則20%・参考資料15%・リンク10%）は**体裁の遵守度**を測っており、Google が見る**独自の情報価値**とは別物。→ 「weighted ≥ 2.5 をQAゲートにする」旧方針は無効（既に満たしている）。判定は**実際の散文密度**で行う。
4. **長い非インデックスページ（過去問・textbook）は「薄い」のではなく「重複/独自性」問題**。過去問CEMは3万字超でも非インデックス。過去問設問文は競合サイト（pejp.net 等）に逐語で存在するため Google が重複と見なす。長くしても直らない。別処置（独自の解説・傾向分析レイヤーの付加）か構造的な割り切りが必要。

---

## 非インデックス265本のバケット分類

| バケット | 本数 | 本文中央値 | 性質 | 処置 |
|---|---|---|---|---|
| **W1 CEMキーワード薄層** | 166 | 2,685字 | 散文が痩せた辞書型（体裁は合格） | 散文増補リライト（最優先） |
| W1b CEMその他 | 7 | 8,133字 | pattern-essay・exam-index 等 | 個別判断 |
| **W2 転職ガイド(civil1)** | 8 | 3,116字 | guide だが未インデックス | guide-rewriter＋fact-check |
| **W3 textbook(civil1)** | 18 | 7,005字 | 長いが独自性課題 | 独自解説の付加 |
| **W4 過去問(civil1)** | 23 | 25,687字 | 重複コンテンツ | 傾向分析レイヤー付加 |
| **W4 過去問(CEM)** | 13 | 32,981字 | 重複コンテンツ | 同上 |
| W5 civil2 | 6 | 29,079字 | 重複コンテンツ | 同上 |
| W6 建設部門 | 7 | 3,992字 | 入口記事 | 個別判断 |
| W7 一次(pe-first-stage) | 9 | 28,827字 | 重複コンテンツ | 同上 |
| W8 コンクリート | 3 | 5,233字 | 下書き系 | 個別判断 |
| Z サイト/カテゴリ | 5 | — | /privacy・category代替canonical | 対応不要（正常挙動） |

全リストは `.tmp/adsense-lists.txt`（本セッション生成、非コミット）に本文字数・weighted・weak_axes 付きで格納。

---

## なぜ「有用性の低いコンテンツ」なのか（因果整理）

AdSense 審査は **人間のレビュアーがサイトをサンプリングして総合価値を判定**する。Google 検索のインデックス判定とは別プロセスだが、**同じ根**を見る:

- **辞書型テンプレの大量生産**: CEMキーワードは640ページが「定義→図→位置づけ→参考資料」の同一テンプレ。各ページの独自散文が130〜数百字だと、レビュアーにも Google にも「量産された薄いページ」に見える。166本が非インデックスなのはこの兆候。
- **過去問の逐語転載**: 過去問設問は公開情報だが競合と重複。解答解説の独自性が薄いと「付加価値の少ないアフィリエイト/スクレイプ的コンテンツ」と判定されうる。
- **サイト全体の希釈**: 良質なガイド記事があっても、薄いページが25%あると平均品質が下がる。

→ **AdSense に効くのは「レビュアーがサンプリングしそうなページ（＝インデックス済みで流入のある実記事）の価値を担保しつつ、薄いページの割合を下げる」こと。** ユーザー方針で noindex は不使用のため、**薄いページを実質的に厚くする**のが唯一の手段。

---

## 対応方針（優先度順）

### 最優先: W1 CEMキーワード薄層（166本）を散文増補

- **QAゲートの変更**: 内部 weighted は使わない（既に合格）。判定は **本文実測字数 + 独自散文の密度**。目標: 各ページ本文 3,000字以上、かつ定義の丸写しでなく「実務での使われ方・試験での問われ方・関連概念との差異」を独自に記述。
- **着手順**: 本文短い順。本文2,000字未満の24本 → 2,500字未満の70本 → 残り。
- **手段**: `keyword-rewriter`（Generator）で増補 → スポットで `cem-qa` ＋ **人手読み**で散文が実質的に増えたか確認（rubric合格だけで満足しない）。事実加筆時は WebSearch 一次情報照合（LLM は試験統計・制度を外す）。
- **規模の正直な見積もり**: 166本の品質リライトは**複数セッションの作業**。本セッションでは最短ページから着手し、完了数と残リストを本レポート末尾に記録する。

### W2 転職ガイド8本 — 実は「薄くない」ので優先度低

実測で `guide-career-salary` は本文3,590字・良質な散文構成。それでも非インデックス（「クロール済み - 未登録」）。**原因は薄さではなく、サイト権威性・鮮度・8本の類似ガイド間の差別化不足**。人間のレビュアーが見れば十分な記事なので **AdSense「有用性」問題ではない**。リライトは低レバレッジ。むしろ8本の切り口重複を相互リンク＋差別化で整理する方が索引に効く可能性（時間をかけて観察）。

### W3/W4/W5/W7 長尺の重複ページ（本質は独自性）

- **長くする処置は無効**（既に7,000〜33,000字）。過去問設問は競合と重複するため索引されにくい構造的問題。各年度ページ冒頭に独自の「出題傾向・分野構成・攻略順序」を付加すれば差別化にはなるが、**AdSense 対策としての優先度は最低**（レビュアーが「有用性が低い」と感じるのは短い量産ページであって、長い過去問解説ではない）。

### 絞り込みの結論

非インデックス265本のうち、**AdSense「有用性の低いコンテンツ」に直接効くのは W1 の薄いCEMキーワードページ（166本＝全体の63%）**。W2（良質）・W3/W4（長尺）は別要因の非インデックスで、審査対策としては後回しでよい。**本セッションの処置は W1 に集中する。**

### 補助（軽量・即効）

- **極小PNG**: 対応不要（実測で解像度十分。q3-fig 507×288 等、200×180の `fig-2-42a/b` のみ小さいが機能）。**画像クロップは審査落ちの主因ではないと確定**。
- **CLS超過2ページ**（`civil-construction-1-primary-r07-a` 0.176 / `pe-comprehensive-management-r07-primary` 0.151）: **計画時の想定「AdSense枠に width/height 明示」は誤り** — コードベースに広告ユニット（`<ins class="adsbygoogle">`）は未実装（`AdSenseScript.tsx` のローダーのみ）で、審査未通過ゆえ広告は描画されない。CLSの実原因は `ArticleImage` のSVG分岐（raw `<img>` + `height:auto` で寸法予約なし、`ArticleImage.tsx:47-56`）が最有力。**これはCWV/ランキングの別課題であり、AdSense「有用性の低いコンテンツ」却下とは無関係。** 個別のCWV調査＋検証を経てから修正すべき（本セッションでは投機的修正をしない）。

---

## 再申請 SOP（ユーザー作業）

1. リライト＆deploy 後、GSC で sitemap 再送信 ＋ 強化した主要URL 10〜20本の手動インデックス登録リクエスト。
2. インデックス反映を1〜2週間観察（非インデックス率が下がるか）。
3. **前回却下から2〜4週間空けて再申請**（短間隔の連続再申請は逆効果）。
4. 既存チェックリスト: `docs/project/_archive/03_civil-adsense-resubmission.md:147-191` を流用。

---

## 処置ログ（本セッション追記）

### 確定事項
- **W1 薄層 112本すべてリライト完遂**（本文3,000字未満だった非インデックスCEMキーワードを全て3,000字超の実質散文へ）。これが AdSense「有用性の低いコンテンツ」への中核処置。次は deploy → GSC 手動索引登録 → 2-4週後に再申請（下記 SOP）。
- 画像クロップ: 主因でないと確定（極小PNG 8枚を実測、解像度十分）→ 対応不要
- CLS修正: 計画の前提誤り（広告ユニット未実装）→ 投機的修正を回避、CWV別課題として切り離し
- W2/W3/W4: AdSense「有用性」問題ではないと判断 → 処置を W1 に集中

### W1 リライト完遂（本文3,000字未満だった薄層 112本すべて）✅

**本セッションで W1 薄層 112本を全て 3,000字超の実質散文へ増補・コミット完了（batch1〜19）。** 全数検証で 112/112 が本文3,000字以上（frontmatter除去・空白除去後）を確認。全て lint HIGH=0・U+FFFD なし・frontmatter revisionCycle +1・キーバリュー表は散文化。

- コミット: `d3087710c`(b1) / `e7c5b960a`(b2) / `b21433663`(b3) / `ef8062190`(b4) / batch5〜19（各6本前後、slug は各コミットメッセージ参照）。
- 全ページ「空の関連概念節を散文で充填・定義の実務文脈化・5管理トレードオフの具体記述」で増補。代表例 pdca-cycle(1,054→4,857)。
- 事実精度: 法令名・年号・条番号は既存記述＋標準テキストmd の範囲のみ、憶測補完禁止。過去問引用は grep 実データのみ（架空記述なし）。

### W1 スコープ精緻化（重要）
W1バケット166本のうち、**リライトで効くのは本文3,000字未満の112本＝全て完遂**。残る54本は既に本文3,000字超で、非インデックスの原因が薄さではない（W2ガイドと同様、権威性/鮮度/重複）ため**リライト対象外**（例: ismap 3,013 / labor-union-act 3,014）。長くしても索引改善にはつながらない。

### 運用知見（次セッション向け・重要）
- **サブエージェント・ストールの主因＝WebFetch/WebSearch**（会社PCプロキシが外部API遮断、[[measurement-incidents]]）。リライトに web は不要 → 全プロンプトで WebFetch/WebSearch 禁止・既存参考URL維持・新規URL追加禁止を明示すると安定。
- keyword-rewriter に3ページ渡すとページ毎サブエージェントへ非同期委譲する（ネスト最大3層観測、コミット後の遅延重複書き込み事故あり）。「あなた自身が直接編集し、サブエージェントを spawn しないこと」を明示すると直接編集する。
- 巨大な過去問ファイル(*-primary は数万字)の Read/grep でもストール → Pattern E を省略し既存本文の散文化＋トレードオフ加筆のみで 3,000字到達させると安定。
- 内部 weighted スコアは Google 索引判定と乖離 → QAゲートに使わず「本文実測字数 + 独自散文密度」で管理。要リライト初期リストは `.tmp/adsense-todo.json`。

### 検証メモ
`next build` は未実行（`build` が `refresh-indexes` を内包し、別セッションが編集中の `src/config/*.json` を巻き込むため回避）。index 再生成は deploy 時に CI が自動実行する。content MDX の妥当性は pre-commit（validate-mdx）＋ lint-mdx-mobile HIGH=0 ＋ U+FFFD チェックで担保済み。
