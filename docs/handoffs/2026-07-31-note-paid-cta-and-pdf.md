# note 有料記事の CTA 位置是正 ＋ 土木 経験記述の PDF 一本化（2026-07-31）

別 PC で続行するための引き継ぎ。**残りは全部 note のブラウザ自動化**なので、note ログイン済みプロファイルのあるマシンで実行する。

## 発端

有料記事 `n74c193d154e5` の無料部分に「土木もくじ」導線が無いのでは、というユーザーの指摘。調べたところ、**有料記事は `paidBoundary` 以降が paywall の中に入るため、記事末尾に置く L2 もくじ CTA が非購入者に一度も見えていなかった**。回遊させたい相手はまさに「買わずに離脱する読者」なので、配置が構造的に間違っていた。`magazines/` は `audit-note-funnel` の D1-D6 スコープ外で、機械監視の死角になっていた。

## 完了済み（コミット済み・ライブ反映済み）

### 1. CTA 位置の是正 — 有料公開記事 577 本

| コミット | 内容 |
|---|---|
| `3e15ad98e` | パイロット 5 本 |
| `1926937f6` | 234 本 ＋ `wire-note-paid-cta.mjs` 新設 ＋ CI ゲート化 |
| `ccf8dfeb7` | 追加 326 本（※別セッションのコミットに同梱された。下記「事故」参照） |
| `5a7c7fcb2` | PDF 178 本 |

- L2 もくじブロックを**有料境界 H2 の直前（＝無料プレビュー末尾）**へ移設／新規挿入
- civil 二次系のみ、空いた末尾（有料域）に**メンバーシップ CTA**（経験記述系と二次学科記述系で文面出し分け。一次＝intent 別、総監/建設部門＝セグメント違反 のため対象外）
- 同一画像の重複除去（note は 2 枚目の CDN 確定に失敗して本文更新が ABORT するため、ライブ反映の前提条件）
- **現在 577 本中 571 本が無料域**（残り 6 本は L2 もくじ自体が未定義の資格＝コンクリート主任技師5・技術士一次1）
- 再発防止: `npm run check-note-paid-cta` を CI（`r2-audit.yml` 全量）と `quality:audit` に追加。`note-funnel-architecture.md` に**原則 2-b** を制定

**ライブ反映も完了**（一周目 214/215・二周目 114/115・再実行 1/1）。

### 2. 土木 経験記述 178 本に印刷用PDFを付与（C-2 第1段）

建設部門 200 本は既に「本文で約束 → 記事 dir に PDF 実体 → note へ添付」で運用済みだったが、土木は PDF が一切なかった。同じ形へ揃える方針（ユーザー判断 C-2＝note へ一本化）。

- `scripts/gen-pdf-specs-civil-keiken.mjs` 新設 — 記事を実読して抽出範囲を決め spec を生成（160 記事分）。CTA は紙面から除外
- `scripts/place-civil-keiken-pdfs.mjs` 新設 — 生成 PDF を記事 dir へ配置し、末尾へ「## 印刷用PDF」節を冪等に追記
- **PDF 178 本（17MB）を配置し、本文の案内もライブ反映済み（178/178 整合）**
- `magazine-to-pdf.mjs` の**タイトル二重印字を根本修正**（`extract()` が H1 を本文先頭に必ず足す一方、`include` 省略時の既定範囲が H1 を含んでいた）。該当 18 本を再生成し 23 本再検査で二重 0

### 3. ついでに落とした既存不具合

- サイト送客リンクの **UTM 欠落 85 件（23 記事）**。原因は `add-note-utm.mjs` の `EXAM_DIRS` が古く建設部門・一次・コンクリート系を解決できなかったこと（85 件中 81 件がその配下）。スクリプトも修正
- `utm_medium=inline` → `referral`（6 記事）、autolink `<url>` 13 件をアンカー付きリンクへ
- 複数行 blockquote 29 箇所（note paste で中身が脱落する形）
- frontmatter の `price` 重複 1 件（工事04）。YAML 重複キーで PDF 生成が落ちていた。全 721 記事走査で同型はこの 1 件のみ

## 残作業

### A. PDF 添付 185 件（最優先・実害あり）

`check-note-attachments --live` を全 457 件で実行済み（取得失敗 0）。**充足 272 / 不足 185**。欠落リストは `.claude/state/note-attachments-missing.json` に生成済み。

**note のファイルアップロードは 1 日 100 件が上限**（2026-06-16 実証。超えると以降が全て ABORT）。最低 2 日かかる。

```bash
node scripts/note-attach-batch.mjs --commit --limit 100
```

`.claude/state/note-attach-done.json`（git 追跡下）に done-log が残るので、翌日そのまま再実行すれば続きから進む。

**先に片付けるべき 7 件**（今日の作業とは無関係な既存の欠落。本文で PDF を約束しているのにライブに無く、**購入者が受け取れていない**）:

- `技術士建設部門/magazines/BK-I_必須科目I` の R07 模範解答 ＋ R8予想①〜⑥

欠落 185 件の内訳: 完全攻略パック104 / 想定工事バンク36 / 2テーマ大全10 / BK-I_必須科目I **7** / 学科記述10 / 完成答案集8 / 過去問模範答案集10。

> 総監模範論文 93 本は**すべて添付済み**だった（欠落リストに 1 件も出ていない）。「PDF を作ったのに配っていない」という当初の推測は外れ。本文で案内していなかっただけ。

### B. ココナラ 4 商品の取り下げ（ユーザー操作）

C-2 は「note へ一本化し、ココナラは人が動くサービス（診断/添削/作成）に絞る」方針。note 側に PDF が付いた以下 4 商品が重複するので取り下げる。

| ココナラ商品 | 価格 |
|---|---|
| 1級土木の経験記述 完成答案集を送ります | ¥3,500 |
| 2級土木の経験記述 完成答案集を送ります | ¥3,000 |
| 1級土木 経験記述の過去問模範答案を送ります | ¥3,000 |
| 2級土木 経験記述の過去問模範答案を送ります | ¥3,000 |

取り下げ後に SSOT を更新する: `src/lib/coconala-services.ts`（status）／`docs/note/1級・2級土木/ココナラ展開キット.md` §2 の価格表／`.claude/knowledge/reference/coconala-operations.md`。整合は `npm run check-coconala-wiring`。

**残り 5 商品**（学科記述攻略 1級2級・予想模試 1級2級・出題分析）は note 側に spec が無いため今回の対象外。やるなら `gen-pdf-specs-civil-keiken.mjs` と同じ方式で spec を起こす。

### C. 積み残し 2 件

**`技術士総監/計算問題パターン集`（ne190c3ef2fca）** — 画像 14 枚。4 回試して毎回 note 側の CDN 確定が 9〜11/14 で止まり fail-closed で中断。押し切ると有料記事の図がライブで欠落する。CTA と PDF 節がソース止まり。画像に重複は無く時間切れでもない（14 枚なら 280 秒待つ設定）。別 PC・別回線で再試行するか、`--img-lenient` を使うかは要判断。

**建設部門 電力土木/鉄道 R08-yosou 6 本** — 本文で印刷用PDFを約束しているのに **PDF 実体が無い**ため `check-note-attachments` が pre-commit を止め、CTA 修正を 2 度とも除外した（現在も未適用）。spec（`BK-09_電力土木.json` / `BK-10_鉄道.json`）は存在するので PDF を生成すれば通る。

```bash
node scripts/magazine-to-pdf.mjs --spec scripts/pdf-specs/BK-09_電力土木.json
node scripts/magazine-to-pdf.mjs --spec scripts/pdf-specs/BK-10_鉄道.json
node scripts/wire-note-paid-cta.mjs --apply
```

## 別 PC での前提

- note ログイン済みプロファイルが要る。無ければ最初に `npm run note-edit-session` で手動ログイン
- `git pull` してから始める。**未 push のコミットは無い**
- 検査系は全て `--live` 無しならオフラインで動く

## 事故・注意（再発防止）

**1. 公開判定の誤りで 326 本を静かに取りこぼした。** 最初 `noteStatus: 'published'` を必須にしたが、**この行を持たない公開済み記事が多数あった**（正しい判定＝`noteId`/`noteUrl` の有無で 577 本、誤った判定では 251 本）。BK-02〜BK-11 と総監模範論文の全ペルソナが丸ごと未検査だった。`check-note-boundary.mjs` と同じ判定に揃えること。

**2. 別セッションのコミットに 326 ファイルが同梱された。** commit しようとした瞬間に別セッションが commit し HEAD が動いて失敗、その間に相手の `ccf8dfeb7`（本来 1 ファイルの競合調査 docs）へ私の変更が巻き込まれた（328 files changed）。**内容の欠落は無い**が、コミットメッセージと中身が食い違っている。並行セッションが動いている最中の reset/amend は過去に作業消失を起こしているので履歴は触っていない。同一ワークツリーでの並行は worktree 分離が正（CLAUDE.md §10）。

**3. `nohup ... &` でバックグラウンド起動したら親シェル終了で道連れになった。** 216 本のバッチが 1 本で止まっていた。harness の `run_in_background` を使うこと。
